import * as AuthTypes from '../actionTypes/auth'

const initialState = {
    formAction: '',
    loggedIn: false
}

// export default function Auth(state=initialState, action) {
//     switch(action.type) {
//         case AuthTypes.
//     }
// }


